name: on_device_model_google_gemma_270_try
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  file_picker: ^10.3.10
  flutter:
    sdk: flutter
  flutter_gemma: ^0.12.8
  flutter_markdown: ^0.7.7+1
  image_picker: ^1.1.2
  path_provider: ^2.1.5
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
